<% float = picture_counter % 10 < 5 ? "left" : "right" %>
<% width = picture_counter % 5 == 0 ? 50 : 25 %>
<li>
  <%= link_to image_tag(picture.photo.url(:large), class: "picture", style: "width:#{width}%; float:#{float};"), picture_path(picture) %>
</li>
